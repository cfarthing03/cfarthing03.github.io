V2 layout correction
This version uses the same structural class hierarchy as the supplied current Sin pages:
- deity pages: Lust / Pride
- religion pages: The Passions / The Gladiators

The earlier V1 inline CSS was redefining the shared grid/hero layout from styles.css.
V2 removes those structural overrides and leaves only page-specific decoration inline.
Replace the previous 14 Virtue HTML files with these; the PNG asset names remain unchanged.
